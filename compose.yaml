services:
  postgres:
    image: postgres:17-alpine
    container_name: whos-in-postgres
    restart: unless-stopped

    environment:
      POSTGRES_USER: ${POSTGRES_USER:-whosin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-whosin}
      POSTGRES_DB: ${POSTGRES_DB:-whosin}

    ports:
      - "${POSTGRES_PORT:-5432}:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-whosin} -d ${POSTGRES_DB:-whosin}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data: